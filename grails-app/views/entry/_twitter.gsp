<img src="${entry.imageUrl}" alt="${entry.author}"/>
        <h4>${entry.content}</h4>
        <a href="http://twitter.com/${entry.author}" target="_blank">@${entry.author}</a>
            |
        <g:formatDate date="${entry.placedDate}"/>
            |
        <a href="${entry.sourceUrl}" target="_blank">Link</a>